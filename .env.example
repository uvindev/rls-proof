# Optional. Enables aggregate page-event delivery through your own Plausible site.
# Migration SQL and audit results are never included in analytics events.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Optional. Replace the pilot email path with a configured Team checkout URL.
NEXT_PUBLIC_TEAM_URL=
